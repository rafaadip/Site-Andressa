# ── Banco ────────────────────────────────────────────────
DATABASE_URL="postgresql://usuario:senha@host:6543/postgres"
DATABASE_URL_UNPOOLED="postgresql://usuario:senha@host:5432/postgres"
# Usado só pelos testes de integração; deixe vazio para pulá-los.
DATABASE_URL_TEST=""

# ── Auth (admin) ─────────────────────────────────────────
AUTH_SECRET=""                      # openssl rand -base64 32
AUTH_URL="http://localhost:3000"
ADMIN_EMAIL="andressa15correia@gmail.com"

# ── Google Calendar ──────────────────────────────────────
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_CALENDAR_ID="primary"
GOOGLE_WEBHOOK_TOKEN=""             # openssl rand -hex 16

# ── Criptografia ─────────────────────────────────────────
ENCRYPTION_KEY=""                   # openssl rand -base64 32 (32 bytes)
TOKEN_SALT=""                       # openssl rand -hex 16

# ── E-mail ───────────────────────────────────────────────
RESEND_API_KEY="re_..."
EMAIL_FROM="Dra. Andressa Correia <contato@exemplo.com.br>"
EMAIL_REPLY_TO="andressa15correia@gmail.com"

# ── Agendamento ──────────────────────────────────────────
# Sem Google Calendar (FASE-05), o site oferta horários SEM saber dos
# plantões da médica. Em produção isso exige decisão consciente:
# AGENDAMENTO_SEM_GOOGLE="aceito". Sem isso, /agendar cai no WhatsApp.
AGENDAMENTO_SEM_GOOGLE=""

# ── Cron ─────────────────────────────────────────────────
CRON_SECRET=""                      # openssl rand -hex 16

# ── Público ──────────────────────────────────────────────
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_WHATSAPP="5511998053826"
